#include <iostream>
using namespace std;
int n, m, a[100005], b;

bool check (int x)
{
    return x * b
}

int main ()
{
    cin >> n >> m;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
        b += a[i];
    }
    int left = 0, right = 1000000005, mid = 0;
    while (left < right)
    {
        mid = (left + right) >> 1;
        if (check(mid))
    }
}
